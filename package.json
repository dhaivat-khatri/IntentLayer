{
  "name": "intentlayer-backend",
  "version": "1.0.0",
  "description": "IntentLayer LLM backend — proxies all AI calls server-side",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^4.18.2",
    "node-fetch": "^2.7.0"
  }
}
